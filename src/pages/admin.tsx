import { Layout } from "@/components/Layout";

export const Admin = () => {
  return (
    <Layout>
      <div className="container mx-auto px-4 py-8">
        <h1 className="text-2xl font-bold">لوحة التحكم - Admin</h1>
        <p className="text-muted-foreground">إدارة التطبيق والمستخدمين (قريباً).</p>
      </div>
    </Layout>
  );
};
